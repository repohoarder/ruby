<p id="notice"><%= notice %></p>

<h1>Payment Methods</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Merchant</th>
      <th>Merchant</th>
      <th>Cardtype</th>
      <th>Lastfour</th>
      <th>Active</th>
      <th>Default</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @payment_methods.each do |payment_method| %>
      <tr>
        <td><%= payment_method.user_id %></td>
        <td><%= payment_method.merchant %></td>
        <td><%= payment_method.merchant_id %></td>
        <td><%= payment_method.cardtype %></td>
        <td><%= payment_method.lastfour %></td>
        <td><%= payment_method.active %></td>
        <td><%= payment_method.default %></td>
        <td><%= link_to 'Show', payment_method %></td>
        <td><%= link_to 'Edit', edit_payment_method_path(payment_method) %></td>
        <td><%= link_to 'Destroy', payment_method, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Payment Method', new_payment_method_path %>
